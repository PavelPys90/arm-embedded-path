# --- STM32CubeIDE / Embedded Project Files ---
# Build output directories, which contain compiled binaries and object files.
Debug/
Release/
build/

# Executable, binary, and map files generated during compilation.
*.bin
*.elf
*.hex
*.map

# Object and dependency files.
*.o
*.d
*.su

# IDE-specific configuration files that are unique to each developer's environment.
# These files should not be committed to the repository.
.settings/
.project
.cproject
*.launch

# Debugger-related files.
*.jlink
*.gdbtrace

# --- LaTeX Document Files ---
# Auxiliary files generated by LaTeX compilers (e.g., pdftex, latexmk).
# These files are recreated every time the document is built.
*.aux
*.bbl
*.bcf
*.blg
*.brf
*.fls
*.fdb_latexmk
*.idx
*.ilg
*.ind
*.lof
*.log
*.lol
*.lot
*.nav
*.out
*.snm
*.toc
*.synctex.gz
*.vrb
*.glg
*.glo
*.gls
*.acn
*.acr
*.alg

# Temporary files created by text editors or LaTeX tools.
*.bak
*.tmp
*~

# Intermediate and output document files that are regenerated from source.
# The final PDF file is often included for review, but you can ignore it if preferred.
# Uncomment the line below to ignore PDF files.
# *.pdf

# --- Editor and OS-Specific Files ---
# Common files from popular editors (e.g., VS Code).
.vscode/

# Operating system files.
.DS_Store
Thumbs.db
Desktop.in
